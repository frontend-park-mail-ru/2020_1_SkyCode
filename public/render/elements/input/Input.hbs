<input  class="{{classes}}"
        type="{{type}}"

        {{#if placeholder}}
            placeholder="{{placeholder}}"
        {{/if}}

        {{#if src}}
            src="{{src}}"
        {{/if}}

        {{#if max}}
            max="{{max}}"
        {{/if}}

        {{#if min}}
            min="{{min}}"
        {{/if}}

        {{#if id}}
            id="{{id}}"
        {{/if}}

        {{#if value}}
            value="{{value}}"
        {{/if}}

        {{#if isRequired}}
            required
        {{/if}}

        {{#if pattern}}
            pattern="{{pattern}}"
        {{/if}}

        {{#if maxlength}}
            maxlength="{{maxlength}}"
        {{/if}}

        {{#if minlength}}
            minlength="{{minlength}}"
        {{/if}}
/>
