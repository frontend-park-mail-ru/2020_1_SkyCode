<input  class="{{classes}}"
        type="{{type}}"
        placeholder="{{placeholder}}"
        src="{{src}}"
        max="{{max}}"
        min="{{min}}"
        id="{{id}}"
        value="{{value}}"/>