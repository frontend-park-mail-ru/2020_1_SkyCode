<div class="action-bar {{classes}}">
    <div class="action-bar__list">
        {{#each actions}}
            <img src="{{this.src}}" class="action-bar__li" alt="{{this.alt}}">
        {{else}}
            <p>Can't load actions.</p>
        {{/each}}
    </div>
</div>
